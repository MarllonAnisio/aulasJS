const notaDaProva = 6.5;

/**
 * Usando o operador ternário para determinar se o aluno está aprovado ou reprovado
 * voce elimina o bloco if e else do programa e deixa o codigo limpo e objetivo, 
 * se vor verdadeira a condição vai retornar a primeira expressão, se for falsa retorna a segunda expressão
 */
const resultado = notaDaProva >= 7 ? "Aprovado" : "Reprovado";

console.log(`O aluno está: ${resultado}`);

